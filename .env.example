# 你的域名（需已解析到服务器 IP）
DOMAIN=checkin.example.com
# 会话签名密钥，用 `openssl rand -hex 32` 生成一段随机字符串
SESSION_SECRET=please-generate-a-long-random-string
# 推送通知的邮件主题（无需真实邮箱，格式需为 mailto:xxx）
VAPID_SUBJECT=mailto:admin@example.com
